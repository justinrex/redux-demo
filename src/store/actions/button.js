import { INCREMENT_COUNT, INCREMENT_HOVER } from '../constants/button';

export const incrementCount = () => ({ type: INCREMENT_COUNT });
export const incrementHover = () => ({ type: INCREMENT_HOVER });
